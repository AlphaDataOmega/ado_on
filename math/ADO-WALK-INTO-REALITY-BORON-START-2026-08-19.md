# ADO walk into reality — boron start checkpoint — 2026-08-19

This checkpoint marks the proposed start of the ADO walk from abstract nested address mathematics into independently measured chemistry/physics. The walk starts with boron as the first adversarial correspondence target, not as a proven physical Null material.

## Method: walk from both ends

Do not derive chemistry by naming ADO states after known elements, and do not alter ADO rules to fit the periodic table.

Walk independently:

```text
ADO encoding / R4 / scale / pressure / harmonic field
    -> candidate stable address structures

measured chemistry / nuclear structure / spectroscopy
    -> independently known state structures
```

Then compare where the two walks meet.

Boron is the current first checkpoint because ordinary nuclear bookkeeping gives the exact identity/isotope structure:

```text
Boron: Z = 5
10B: 5 protons + 5 neutrons
11B: 5 protons + 6 neutrons
```

Thus elemental identity remains `5` while one contained nuclear count differs by one unit. Under the proposed nested base-100 scale projection, a one-unit difference at a contained scale may appear as a `.01` residual at its containing scale. This is an encoding hypothesis, not the statement that a neutron literally equals `.01`.

## Floor / bubble distinction retained

Freeze the prior checkpoint:

```text
050                 = internal floor / Null seam
050 | 050 | 050     = proposed symmetric three-field Null projection
060                 = first post-seam scalar address
060 | 050 | 060     = completed symmetric bubble off the zero line
-010 | 000 | +010   = centered/oriented representation of that bubble
```

Do not revert to calling `060 | 050 | 060` the Null door itself.

## Hidden trajectory field under equal projection

The three visible `050` projections may retain distinct hidden trajectory/scale addresses. A candidate three-depth example is

```text
050_(.1) | 050_(.01) | 050_(.001)
```

with R4 scale ratios

```text
.1 / .01   = 10
.01 / .001 = 10
.1 / .001  = 100
```

and closure

```text
10 * 10 = 100.
```

Thus equal scalar projection need not imply equal complete address. Hidden trajectory differences can preserve a relational field beneath an apparent Null projection.

## Carbon first completed-bubble candidate

The current ADO-side candidate for the first post-5 completed bubble is carbon (`Z = 6`) with coarse projection

```text
060 | 050 | 060.
```

This is not yet an established carbon encoding. It is frozen as a prediction/correspondence target so subsequent chemistry must not be used to modify it retroactively.

The two outer scalar projections are equal while orientation is opposite around the middle:

```text
-010 | 000 | +010.
```

## Carbon scale walk

Carry the nested scale residuals rather than flattening the address. Let

```text
epsilon_1 = .1
epsilon_2 = .01
epsilon_3 = .001
...
```

The accumulated inward residual through depth `k` is

```text
R_k = sum_(j=1..k) 10^(-j).
```

Therefore

```text
R_1 = .1
R_2 = .11
R_3 = .111
...
R_infinity = 1/9.
```

This permits indefinitely deep address history with bounded accumulated displacement.

The reciprocal scale walk carries pairs such as

```text
.1 <-> 10
.01 <-> 100
.001 <-> 1000
```

with product invariant

```text
|L_k| |R_k| = 1
```

for the instantaneous reciprocal scale pair (notation distinct from the accumulated residual `R_k` above).

The complete bubble therefore carries two phased scale trajectories around the same `050` projection. The scalar projection can remain frozen while the complete TELE/trajectory address evolves.

## Shadow-field candidate

At infinite refinement depth, the accumulated inward residual is bounded:

```text
.1 + .01 + .001 + ... = 1/9.
```

Opposed phased trajectories may therefore carry limiting signed residuals

```text
-1/9 and +1/9
```

with mean zero and nonzero separation

```text
(+1/9 + -1/9)/2 = 0
(+1/9) - (-1/9) = 2/9.
```

This is a candidate mathematical representation of a hidden relational/shadow field under a zero/equal coarse projection. It is not yet a physical electromagnetic or gravitational field identification.

## Water / optical / Tuttle-bubble hypothesis

A separate physical hypothesis motivating later experiments is that a water-based compartment may provide a useful medium for testing multiple balanced channels at one spatial boundary:

- acid/base balance;
- ionic/electrical response;
- dielectric polarization;
- optical refraction / phase delay;
- dissolved-species transport.

The proposed Tuttle-bubble idea is a water-based compartment in which opposed field variables may reach zero net imbalance at a common spatial address while retaining nonzero internal relational structure.

The phrase "TARDIS box" is retained as an informal project nickname for a possible future test chamber. No physical teleportation capability is established.

### Important water correction

Do **not** freeze the statement "water without electricity is viscous" as established physics. Liquid water has ordinary molecular viscosity whether or not an external electric field is applied. Electric fields, dissolved ions, temperature, pressure, confinement, interfaces, and electrohydrodynamic effects can alter water behavior, but a specific ADO pressure/impedance mapping must be derived and measured.

The useful testable hypothesis is narrower:

```text
Does a controlled electrical / ionic / pH / optical field change the effective transport, polarization, or refractive response of a carefully defined water compartment in the manner predicted by the ADO address model?
```

## Boron + water hypothesis status

Boron naturally participates in aqueous chemistry and can retain elemental identity while changing chemical/speciation state. This makes boron an interesting candidate tracer/carrier for identity-through-state-change experiments.

However, the following remain open and must not be promoted to facts:

```text
boron is the physical Null;
boron is uniquely invariant under all inversion;
boron is required for a TELE chamber;
boron is uniquely mobile through scales;
water + boron enables physical teleportation.
```

The stronger experimental approach is to define the transformation first, compare multiple elements/materials blindly, and determine whether boron uniquely preserves the predicted R4/TELE invariant.

## Reality-walk rule

From this checkpoint onward:

1. preserve the nested signed digit -> base-100 -> three-field -> larger-field hierarchy;
2. preserve all scale residuals (`.1`, `.01`, `.001`, ...); 
3. keep scalar projection separate from complete trajectory address;
4. keep orientation separate from scalar magnitude;
5. freeze ADO predictions before inspecting the next chemistry target;
6. compare against measured nuclear/electronic/spectroscopic structure;
7. log failures rather than changing the encoding to make an element fit.

This file marks the beginning of the explicit "walk into reality" with boron as the first checkpoint and carbon as the first completed-bubble candidate.