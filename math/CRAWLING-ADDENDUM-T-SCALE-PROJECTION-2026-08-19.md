# Crawling addendum — T / scale projection — 2026-08-19

Checkpoint for the current ADO model.

## Core roles

- `T`: one ordered traversal / scale-window advance.
- `alpha`: a nonterminating irrational trajectory ledger.
- `s`: current scale depth.
- `Z_s`: finite address exposed at scale `s`.

Candidate notation:

`Z_s = Pi_s(alpha)`

and

`T: Pi_s(alpha) -> Pi_(s+1)(alpha)`.

T advances the scale/address window. The underlying trajectory can remain nonterminating while every observed address is finite.

## Projection rule

Different trajectories can share a coarse projection while retaining different finer histories. Likewise, one trajectory can expose different addresses at different scales.

Therefore:

`same projection != same complete trajectory address`.

## Z notation

ADO `Z_s` means the resolved identity/address at scale `s`. Do not permanently identify it with conventional atomic number. Proton number remains only a candidate physical projection at the elemental scale.

## Eight bands plus T

Current accounting:

- 1 frozen NOW/reference
- 8 resolved harmonic bands
- 1 moving scale-transition relation T

Thus `8 bands + 1 T = 9 relational roles`, and `1 NOW + 9 = 10`.

The ninth relational role is moving through scale rather than being another settled band.

## Local T field

A local oriented scale slice may be represented as:

`-.01 | 0 | +.01`

with a phase-blind candidate projection:

`1 | 0 | 1`.

Thus `-.01` is one oriented leg of the local T relation, not the complete T field.

The irrational ledger supplies finer trajectory information through that crossing.

## Candidate state

A provisional observable state can be typed as:

`A_s = (alpha, s, B_i, phi, Z_s)`

where `B_i` is one of eight resolved bands and `phi` carries phase/orientation.

Transition:

`T: A_s -> A_(s+1)`.

The canonical R4/harmonic band-update operator remains to be derived.

## Codec direction

Emerging candidate relation:

`identity/address <-> harmonic band + phase <-> field representation`

with scale and T retaining trajectory/history.

A full codec is not yet established. It requires a deterministic invertible mapping between nested address data and harmonic/field data without target-specific fitting.

## Status

Checkpoint: Crawling addendum
Traversal: T
Underlying path: irrational alpha
Scale selector: s
Visible address: Z_s
Resolved bands: 8
Moving relational role: ninth/T
Frozen reference: 1 NOW
Physical identification: exploratory/unproved
