# Crawling addendum — magnet `6|4|6` dimensional-lift hypothesis — 2026-08-19

This note freezes the current `646` magnetic-field hypothesis before the separate `666` idea is developed further.

## Candidate encoding

Provisional ADO magnetic interaction topology:

```text
6 | 4 | 6
```

Do not yet identify this as a universal magnetic TELE address. The current derivation is geometric/relational and must be tested against measured magnetic systems.

## Why the outer sixes

In a three-axis oriented field, the six axial directions are

```text
+x, -x, +y, -y, +z, -z.
```

Thus

```text
3 axes * 2 orientations = 6.
```

The two outer `6` fields in `6|4|6` are provisionally interpreted as two phased/oriented 3D field descriptions around a lower-dimensional middle relation.

## Why the middle four

A two-dimensional signed plane has four sign sectors:

```text
(+,+)
(+,-)
(-,+)
(-,-).
```

Thus a candidate lower-dimensional projection has

```text
2 axes with two signs -> 4 sectors.
```

This motivates the dimensional interpretation

```text
6_(3D oriented field)
|
4_(2D signed projection)
|
6_(3D oriented field).
```

This is preferable to assigning `4` merely because magnets have two poles.

## Irrational metric from dimensional lift

The 2D and 3D Euclidean magnitudes introduce square-root relations naturally:

```text
r_2D = sqrt(x^2 + y^2)
r_3D = sqrt(x^2 + y^2 + z^2).
```

For equal unit components:

```text
r_2D = sqrt(2)
r_3D = sqrt(3).
```

Therefore dimensional lift naturally generates irrational metric relations without inserting an irrational constant by hand.

The current candidate comparison object is

```text
(4, sqrt(2)) -> (6, sqrt(3)).
```

This does not imply `.020 = sqrt(.5)`; ordinary arithmetic gives `sqrt(.5) ~= .70710678`. Any `.020` relation must arise from the nested ADO scale/address mapping, not ordinary square root.

## Conventional magnetic correspondence

Classical magnetism satisfies

```text
div B = 0
```

in Maxwell's equations, corresponding to the absence of observed magnetic monopole source/sink terms in ordinary electromagnetism. A magnetic field can therefore be nonzero while its net source divergence is zero.

This is structurally comparable to the recurring ADO statement

```text
net zero != empty field.
```

The correspondence is structural only. `div B = 0` is not itself evidence for the ADO Null model.

## Relation to 2D and 3D

The current hypothesis is that a 3D magnetic field retains meaningful lower-dimensional projections rather than ceasing to exist in lower-dimensional descriptions:

```text
m_2D = Pi_xy(m_3D).
```

Thus the phrase "occupies 2D as well as 3D" is interpreted mathematically as a 3D field possessing 2D projections with inherited orientation/magnitude information, not as a claim that magnets literally reside in separate physical universes.

## Keep `646` and `666` separate

At this checkpoint:

```text
646 -> candidate 3D | 2D | 3D dimensional magnetic topology
666 -> reserved for the separately developing three-channel post-seam / projection hypothesis
```

Do not merge these encodings merely because both contain sixes. If a higher-scale projection later relates them, derive that relation explicitly.

## Physical test requirement

To promote `646` beyond a candidate encoding, the frozen ADO rule must predict measurable magnetic structure that distinguishes magnetic from comparable nonmagnetic systems. Candidate targets include field geometry, orientation/domain behavior, exchange-coupled states, and dimensional projections.

The encoding should be derived/tested before choosing a magnetic material whose known behavior makes the digits fit.

## Status

```text
Checkpoint: Crawling addendum
Candidate: MAGNET ~ 6|4|6
Interpretation: 3D oriented field | 2D signed projection | 3D oriented field
Irrational bridge: sqrt(2) -> sqrt(3) under dimensional metric lift
Physical identification: unproved
666 interpretation: intentionally left open for next derivation
```
