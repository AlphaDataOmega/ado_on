# Crawling addendum — `.01` ball-crack operator — 2026-08-19

This checkpoint freezes the current role assignment for `.01` in the ADO membrane/lens/bubble model. It is an exploratory mathematical operator, not an established physical constant.

## Core role assignment

```text
050 -> invariant medial surface / center
666 -> bidirectional opposed membrane / relational field
.01 -> elementary scale-resolved crack-opening displacement
T   -> carries the displacement through nested scale
```

The `.01` quantity is not itself the bubble and is not identified with a physical decay probability.

## Crack operator

Let the invariant center be `M`. Define a symmetric crack operator of one-sided size `epsilon`:

```text
C_epsilon(M) = (M-epsilon, M+epsilon).
```

At the current base-100 resolution,

```text
epsilon = .01.
```

Starting from coincident membranes,

```text
(M,M)
```

the crack operation produces

```text
(M-.01, M+.01).
```

The midpoint is preserved exactly:

```text
[(M-.01)+(M+.01)]/2 = M.
```

Thus the crack opens separation without moving the invariant center.

## Bubble width

The membrane-to-membrane width is

```text
B = (M+epsilon) - (M-epsilon) = 2 epsilon.
```

For `epsilon=.01`:

```text
B=.02.
```

Keep the distinction:

```text
.01 -> one-sided displacement / crack quantum at this resolution
.02 -> bilateral membrane separation produced by one symmetric crack.
```

## RH-centered form

Under the current RH normalization,

```text
M = 050 -> 1/2.
```

One crack gives

```text
rho_- = .49 + iT
rho_+ = .51 + iT.
```

Their midpoint remains

```text
(rho_- + rho_+)/2 = .50 + iT.
```

So the crack creates a nonzero transverse bubble around the critical-line center without moving that center.

## Relation to reflected harmonic imbalance

For

```text
s = 1/2 + delta + it,
```

the previously derived reflected logarithmic imbalance for index `n` is

```text
P_n(delta) = 2 delta log(n).
```

If one crack corresponds to

```text
delta=.01,
```

then

```text
P_n = .02 log(n).
```

Thus the bilateral bubble width and the reflected logarithmic imbalance are linked by

```text
P_n = B log(n).
```

This is exact once `B=2 delta` is adopted.

## Relation to `.03`

Keep `.03` separate from the crack:

```text
3 * .01 = .03.
```

Under the separately logged three-channel `n=2` construction, `.03` is a candidate three-channel coupling/amplitude scale generated from three `.01` units. It is not the bilateral bubble width.

Current role separation:

```text
.01 -> elementary one-sided crack displacement
.02 -> full symmetric bubble width
.03 -> candidate three-channel n=2 coupling/amplitude
```

Any physical interpretation of `.03` as gravitational or other coupling remains unproved.

## Scale traversal

The crack may be represented at progressively finer nested resolutions:

```text
.01 -> .001 -> .0001 -> ...
```

The ADO hypothesis is that `T` carries trajectory information through these nested scales while the invariant center can remain fixed.

Do not infer that the physical membranes literally remain separated by these decimal distances; these are address/scale coordinates unless an independent physical mapping is established.

## Closure operator

The inverse operation closes the symmetric crack:

```text
C_epsilon^-1(M-epsilon, M+epsilon) -> (M,M).
```

So the abstract cycle is

```text
closed center
 -> symmetric crack
 -> finite bubble
 -> inverse closure
 -> closed center.
```

The constituent/trajectory history may remain encoded even when the visible separation returns to zero, preserving the established rule

```text
zero projection/resultant != empty internal state.
```

## Current compact architecture

```text
050 : invariant medial surface
666 : bidirectional membrane / relational structure
.01 : elementary crack-opening displacement
.02 : bilateral bubble width
.03 : three-channel n=2 amplitude/coupling candidate
T   : nested-scale trajectory
```

## RH proof status

This construction does not prove RH. In the RH analogy, RH would require showing from the completed zeta/xi mathematics that a nontrivial zero cannot sustain a nonzero bubble width `B` around the invariant `1/2` medial surface.

The missing implication remains a theorem derived from xi/zeta itself, not from the ADO geometry.

## Status

```text
Checkpoint: Crawling addendum
Operator: .01 symmetric crack displacement
Center: 050 / 1/2
One-sided displacement: .01
Bilateral width: .02
Three-channel amplitude: .03 (separate role)
Physical identification: exploratory / unproved
```
